{
  "name": "@soto/ipc",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Soto IPC trust boundary — generic IpcRouter + the command policy/registry. Layered on @soto/core; zero Electron deps.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^4.0.16"
  },
  "dependencies": {
    "@soto/core": "workspace:*",
    "zod": "^3.24.1"
  }
}
